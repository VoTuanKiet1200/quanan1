<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Test Rellax Z-Index</title>
    <style>
        * {
            box-sizing: border-box
        }

        body {
            margin: 0;
            font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
            background: #0b1020;
            color: #eaf0ff
        }

        header {
            position: sticky;
            top: 0;
            z-index: 20;
            padding: 12px 16px;
            background: rgba(5, 8, 18, .75);
            backdrop-filter: blur(10px)
        }

        .wrap {
            max-width: 900px;
            margin: 0 auto;
            padding: 22px 16px
        }

        .stage {
            position: relative;
            min-height: 140vh;
            /* đủ dài để scroll */
            margin-top: 16px;
            padding: 24px;
            border-radius: 18px;
            background: rgba(255, 255, 255, .06);
            border: 1px solid rgba(255, 255, 255, .10);
            overflow: hidden;
        }

        .spacer {
            height: 50vh
        }

        .box {
            width: min(560px, 92vw);
            margin: 22px auto;
            padding: 18px 16px;
            border-radius: 16px;
            background: rgba(255, 255, 255, .10);
            border: 1px solid rgba(255, 255, 255, .14);
            font-weight: 650;
            text-align: center;
            position: relative;
            /* z-index mới có ý nghĩa rõ */
            will-change: transform;
        }

        /* cho 2 khối chồng lên nhau để thấy z-index */
        .overlap {
            margin-top: -190px;
        }

        .box:first-of-type {
            opacity: .75;
        }

        .overlap {
            opacity: .95;
        }

        /* thay -42px */

        /* Ảnh chồng lớp để test z-index */
        .layer-img {
            position: absolute;
            width: 220px;
            height: auto;
            opacity: 1;
            pointer-events: none;
            user-select: none;
            will-change: transform;
            filter: drop-shadow(0 10px 30px rgba(0, 0, 0, .35));
        }

        /* vị trí từng ảnh */
        .img-1 {
            left: 10%;
            top: 18%;
        }

        .img-2 {
            left: 40%;
            top: 120px;
        }

        .img-3 {
            left: 40%;
            top: 120px;
        }
    </style>
</head>

<body>
    <header>Test Rellax z-index</header>

    <div class="wrap">
        <div class="stage">
            <div class="spacer"></div>

            <div class="rellax box">
                I’m that default chill speed of "-2" and default z-index of 0
            </div>

            <div class="rellax box overlap" data-rellax-speed="7" data-rellax-zindex="12">
                I’m super fast!! And on top of the previous element, I'm z-index 5!!
            </div>


            <!-- Ảnh test z-index (chồng lớp) -->
            <img class="rellax layer-img img-1" src="/images/bg.png" data-rellax-speed="-2" data-rellax-zindex="1"
                alt="" />

            <img class="rellax layer-img img-2" src="/images/a.jpg" data-rellax-speed="4" data-rellax-zindex="6"
                alt="" />

            <img class="rellax layer-img img-3" src="/images/mid.png" data-rellax-speed="-6" data-rellax-zindex="4"
                alt="" />

        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/rellax/1.12.1/rellax.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            new Rellax(".rellax", {
                round: true
            });
        });
    </script>
</body>

</html>